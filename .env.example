# Node identification
NODE_ID=node1
NODE_HOST=0.0.0.0
NODE_PORT=8001

# Peer nodes (comma-separated host:port)
PEER_NODES=localhost:8002,localhost:8003

# Redis configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# Raft consensus parameters (milliseconds)
RAFT_ELECTION_TIMEOUT_MIN=150
RAFT_ELECTION_TIMEOUT_MAX=300
RAFT_HEARTBEAT_INTERVAL=50

# Queue configuration
QUEUE_REPLICATION_FACTOR=2
MESSAGE_PERSISTENCE_PATH=./data/queue

# Cache configuration
CACHE_SIZE=1000
CACHE_EVICTION_POLICY=LRU

# Logging
LOG_LEVEL=INFO

# Metrics
METRICS_PORT=9090
